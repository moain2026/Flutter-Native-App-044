# سجل الإصدارات — YECO

> الحزمة: `com.yecomarket.shop` · المستودع: `moain2026/Flutter-Native-App-044`
> الصيغة: `versionName (versionCode)` — `versionCode` يزيد بواحد مع كل إصدار ويُقرأ من `pubspec.yaml` (`version: X.Y.Z+N`).
> **الإصدار الحالي: 1.0.0 (1)** · التالي المقترح: `1.0.1+2` أو `1.1.0+2`.

| الإصدار | versionCode | التاريخ | Git tag | APK | ملاحظات |
|---|---|---|---|---|---|
| **1.0.0** | **1** | 2026-09-17 | `v1.0.0` | `releases/YECO-v1.0.0.apk` | الإصدار الأول الكامل — [`docs/RELEASE_NOTES_1.0.0.md`](docs/RELEASE_NOTES_1.0.0.md) |

---

## [1.0.0] — 2026-09-17 · build 1

### أُضيف
- **المتجر**: 6 تصنيفات × 4 = 24 منتجاً ببيانات واقعية وصور، رئيسية (بانر، تصنيفات، مميزة، عروض، جديد)، قائمة بحث/تصفية/ترتيب/شبكة-قائمة، تفاصيل مع Hero ومراجعات CRUD.
- **السلة والطلبات**: كميات، حذف بالسحب، 3 كوبونات، توصيل مجاني فوق 300، إتمام طلب بـ Form وطريقة دفع، خصم المخزون في transaction، تتبع بخط زمني، إلغاء يُعيد المخزون.
- **الأدوار**: زبون/بائع عند التسجيل؛ لوحة بائع (6 إحصاءات + الأكثر مبيعاً + توزيع التصنيفات)، إدارة منتجات (إضافة/تعديل/حذف/تمييز، صورة من المعرض/الكاميرا)، إدارة الطلبات بتغيير الحالة.
- **المصادقة بالبريد**: بريد → موجود؟ كلمة مرور + OTP بالبريد / غير موجود → إنشاء حساب بالبريد نفسه؛ إنشاء حساب بعد تأكيد الرمز؛ نسيت كلمة المرور بـ 3 خطوات؛ Gmail SMTP عبر `mailer` مع `--dart-define`؛ وضع معاينة عند تعذّر الإرسال.
- **الأمان**: PBKDF2 لكلمات المرور، OTP مجزّأ بـ salt، تحقق بخطوتين قابل للإيقاف، `KeyboardResumeFix`.
- **الترخيص عن بُعد**: `license.json` (active/code/message)، API أولاً ثم raw، 404 قفل نهائي، بلا إنترنت آخر حالة.
- **الإعدادات**: اسم المطوّر واسم المتجر قابلان للتعديل ومحفوظان في SQLite.
- **الحسابات**: تعديل البيانات، تغيير كلمة المرور، تسجيل الخروج، حذف الحساب (CASCADE).
- **الهيكل**: 4 تبويبات بـ Navigator مستقل، زر الرجوع خطوة بخطوة، حوار خروج، شارات السلة/المفضلة.
- **الأصول**: أيقونة متكيّفة بلا مربعات سوداء، خط Tajawal، صور JPEG مضغوطة.
- **الاختبارات**: 33 (ترخيص، OTP، PBKDF2، SQLite CRUD، تخطيط 360×780، تدفقات).
- **التوثيق**: README، BUILD_GUIDE، ARCHITECTURE، SESSION_LOG، RELEASE_NOTES، سكربت `tool/build_release.sh`.

### البناء
- أندرويد فقط (`--platforms android`)، `android-arm64`، `minify` + `shrinkResources` → **21 MB**.
- موقّع بمفتاح الإصدار `release-key.jks` (alias `release`, SHA-256 `58:7A:F4:F3…`).
